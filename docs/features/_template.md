> **이 파일은 작성 템플릿이다.** 새 기능 문서는 이 파일을 복사해
> `docs/features/<scenario-name>.md`(영어 kebab-case)로 만든다.
> 아래 `<...>` 자리표시자를 모두 채운다. **코드에서 확인 못 한 사실은 채우지 말고
> "미확인"으로 두거나 멈추고 사람을 호출한다(추측 금지).**
>
> 이 레이어는 descriptive 문서다 — 규범의 답은 통합본/`adr/`. 작성 규칙은 `README.md` §2 참조.

# 기능: <시나리오 한 줄 이름>

## 0. 메타 헤더

| 필드 | 값 | 비고 |
|---|---|---|
| 기능 ID | `<scenario-name>` | 파일명과 일치 |
| 시나리오 한 줄 | `<사용자가 보는 동작 1줄>` | 인덱스 표와 동일 |
| 관여 repo | `<hub / script-agent / infra 중 해당>` | |
| 기준 meta commit | `<full-40-hash>` | 이 문서가 참조한 통합본/`adr/` 등 **spec의 시점**을 고정한다(`git rev-parse HEAD`). **형제 repo 코드 시점이 아니다** — 코드가 실제 동작한 시점 증거는 §9의 e2e 결과. 본문 코드 앵커로 쓰지 않는다(README §2) |
| 최종 갱신일 | `YYYY-MM-DD` | |
| 검증 상태 | `<검증됨 / 부분검증 / 미검증>` | §9 최종 검증 기준과 연동 |

## 1. 기능 개요

<이 기능이 무엇을 하는가 — 2~4줄. 사용자가 얻는 결과 중심.>

## 2. 사용자 시나리오

<이 레이어의 단위 = 수직 슬라이스. 사용자가 보는 동작 순서를 개조식으로.>
- <단계 1 — 사용자 행동/관찰>
- <단계 2>
- <결과 — 사용자가 최종으로 보는 것>

## 3. 관련 spec 참조 (규범 문서 포인터)

<이 흐름의 규범적 근거. 이 레이어는 결정하지 않고 포인터만 단다.>
- 통합본(`docs/master-design.md`): `§<n>` <항목>
- ADR: `adr/<n>-<slug>.md` <결정 요지>
- 페이로드/봉투: `docs/kafka-payloads.md` / `docs/envelope.md` <해당 절>

## 4. 관여 repo·컴포넌트

<흐름에 참여하는 repo와 그 안의 컴포넌트(모듈/패키지 경계). 라인번호 금지.>
- `script-agent`: <패키지/모듈>
- `infra`: <설정 파일·서비스>
- `hub`: <패키지/모듈>

## 5. 흐름 따라가기 (hop-by-hop)

<이 레이어의 핵심. 한 hop = 한 행. 토픽과 진입점 식별자를 인접 배치해 소스 추적을 직접 안내.
토픽이 없는 hop(비메시징)이면 토픽 열에 `-` 한 글자만 적는다.>

| 단계 | 컴포넌트 / repo | 진입점 식별자 | 토픽 | 관찰 지점(로그/상태) |
|---|---|---|---|---|
| 1 | `<repo>:<package>` | `<Class.method 또는 func>` | `<topic-name>` | `<로그 키워드 / 상태 저장소>` |
| 2 | | | | |
| … | | | | |

## 6. 흐름 다이어그램 (선택)

<cross-repo 흐름 시각화가 도움이 되면 ASCII 시퀀스/파이프라인. 단순하면 생략.>

```
<script-agent> --push--> <collector> --[topic]--> <hub: decoder> --> <store>
```

## 7. 검증 방법

<이 흐름이 동작함을 어떻게 확인하는가.>
- e2e: `e2e/results/<timestamp>.md` §<n> <검증 항목>
- golden case / 단위 테스트: <hub mvn test 케이스 / script-agent go test 케이스>

## 8. 미구현·잔여 사항

<이 흐름에서 아직 구현 안 된 부분. 미구현 spec은 통합본의 영역 — 여기엔 "현재 없음"만 표기하고 미래 설계는 쓰지 않는다.>
- <잔여 1 — 통합본 §<n> 도달 목표, 현재 미구현>
- 없으면 "해당 없음".

## 9. 최종 검증 기준 (필수)

<이 문서의 서술이 어느 시점 무엇으로 사실 확인됐는지 단정.>
- **기준 e2e 결과**: `e2e/results/<timestamp>.md` (PASS <n>/0/0) — 형제 repo 코드가 실제 동작한 시점 증거
- **spec 참조 시점**: §0 기준 meta commit `<hash>` (통합본/`adr/`)
- <기타 확인 근거>
