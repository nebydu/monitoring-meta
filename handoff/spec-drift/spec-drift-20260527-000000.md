# spec-drift 보고서

- **생성일**: 2026-05-27
- **생성자**: spec-sync sub-agent
- **기준 timestamp**: 20260527-000000

---

## 1. 문서 인벤토리 (존재 여부 매핑)

### 1.1 정본 (monitoring-meta/docs/)

| 파일 | 존재 여부 | 비고 |
|---|---|---|
| `docs/통합본_v0_9.md` | 존재 | Phase 1+ 목표 spec, v0.9 |
| `docs/kafka-payloads.md` | 존재 | 8 토픽 payload 스키마 |
| `docs/envelope.md` | **부재** | 아직 작성되지 않음 |

### 1.2 사본 — hub (`../hub/docs/`)

| 파일 | 존재 여부 | 비고 |
|---|---|---|
| `docs/monitoring-demo-message-spec-v0.2.1.md` | 존재 | Phase 0 ground truth (데모 spec 정본 위치) |
| `통합본_v0_9.md` 사본 | **부재** | hub에 통합본 사본 없음 |
| `kafka-payloads.md` 사본 | **부재** | hub에 kafka-payloads 사본 없음 |

### 1.3 사본 — script-agent (`../script-agent/docs/`)

| 파일 | 존재 여부 | 비고 |
|---|---|---|
| `docs/monitoring-demo-message-spec-v0.2.1.md` | 존재 | 데모 spec 사본 |
| `통합본_v0_9.md` 사본 | **부재** | script-agent에 통합본 사본 없음 |
| `kafka-payloads.md` 사본 | **부재** | script-agent에 kafka-payloads 사본 없음 |

---

## 2. 정본 ↔ 사본 drift 분석

### 2.1 통합본 v0.9 (`docs/통합본_v0_9.md`)

**정본**: `monitoring-meta/docs/통합본_v0_9.md` (존재)
**사본**: hub 및 script-agent 모두 사본 없음

**분류: 일치 (사본 자체가 없음)**

통합본 v0.9는 Phase 1+ 목표 spec이며, 현재 계획상 hub/script-agent가 이 문서의 사본을 보유하는 구조가 아니다. 각 repo는 handoff 파일을 통해 작업 spec을 받는 방식이므로 이 상태는 설계 의도에 부합한다. drift 없음.

### 2.2 kafka-payloads.md (`docs/kafka-payloads.md`)

**정본**: `monitoring-meta/docs/kafka-payloads.md` (존재)
**사본**: hub 및 script-agent 모두 사본 없음

**분류: 일치 (사본 자체가 없음)**

통합본과 동일하게, kafka-payloads.md도 형제 repo가 사본을 보유하는 구조가 아니다. drift 없음.

### 2.3 envelope.md (`docs/envelope.md`)

**정본**: **부재** — `monitoring-meta/docs/envelope.md` 파일이 존재하지 않는다.

**분류: 정본 미작성 (findings 항목)**

envelope 작업은 아직 진행 중이며, 본 정본 파일이 작성되지 않은 상태다. 사본 비교 대상 자체가 없다.

---

## 3. 데모 spec 사본 간 drift (hub 정본 ↔ script-agent 사본)

**정본 위치**: `../hub/docs/monitoring-demo-message-spec-v0.2.1.md`
**사본 위치**: `../script-agent/docs/monitoring-demo-message-spec-v0.2.1.md`

### 3.1 파일 수준 비교 결과

두 파일을 전체 내용(506라인) 대조한 결과, **완전 일치**한다.

확인한 항목:

| 점검 항목 | hub 정본 | script-agent 사본 | 일치 여부 |
|---|---|---|---|
| 파일명 | `monitoring-demo-message-spec-v0.2.1.md` | `monitoring-demo-message-spec-v0.2.1.md` | 일치 |
| 버전 표기 | v0.2.1 | v0.2.1 | 일치 |
| 변경 이력 (8장) | v0.1 / v0.2 / v0.2.1 | v0.1 / v0.2 / v0.2.1 | 일치 |
| 토픽 목록 (1장) | commands / job-results / audit-events / heartbeats | 동일 | 일치 |
| envelope 헤더 (2.2장) | x-message-id / x-message-version / x-source / x-trace-id | 동일 | 일치 |
| payload 구조 (5장) | SCRIPT_JOB / LOG_JOB 명세 포함 | 동일 | 일치 |
| ADR 후보 리스트 (7장) | ADR #1~#18 | 동일 | 일치 |
| 총 라인 수 | 506 | 506 | 일치 |

**분류: 일치 — drift 없음**

---

## 4. 종합 요약

| 비교 쌍 | 분류 | 비고 |
|---|---|---|
| `통합본_v0_9.md` — hub 사본 | 일치 (사본 없는 것이 정상) | drift 없음 |
| `통합본_v0_9.md` — script-agent 사본 | 일치 (사본 없는 것이 정상) | drift 없음 |
| `kafka-payloads.md` — hub 사본 | 일치 (사본 없는 것이 정상) | drift 없음 |
| `kafka-payloads.md` — script-agent 사본 | 일치 (사본 없는 것이 정상) | drift 없음 |
| `envelope.md` | 정본 미작성 | findings 항목. drift 비교 불가 |
| 데모 spec v0.2.1 (hub 정본 ↔ script-agent 사본) | **일치** | drift 없음 |

**escalation 필요 항목**: 없음
**critical / conflict**: 없음

---

## 5. Findings 항목

1. `docs/envelope.md` 미작성 — 정본 파일 자체가 없으므로 사본 drift 비교 불가. envelope 작업 완료 후 재검사 필요.
2. hub 및 script-agent는 통합본 v0.9 / kafka-payloads.md의 사본을 보유하지 않는 구조임을 확인. 이는 현재 설계(handoff 기반 분배)와 일치하므로 이상 없음.
3. 데모 spec v0.2.1은 hub(정본)와 script-agent(사본) 간 내용 완전 일치 확인.
