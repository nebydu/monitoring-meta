# spec-drift 보고서 — phase1-044 (T4-5 계약 문서 1차 정합화)

> **생성일**: 2026-06-13
> **work-id**: phase1-044
> **기준 변경**: 기준 문서 `docs/kafka-payloads.md` · `docs/envelope.md` 내 구 분할파일 stale 참조 5곳 현행화 (계약 내용 무변경, 경로·§ 표기만 정정)
> **검사 범위**: 기준 문서 ↔ 형제 repo 사본(`../hub/docs/`, `../script-agent/docs/`)
> **검사 목적**: 이번 정정으로 현행화된 참조 표기가 형제 repo 사본에도 stale 상태로 잔존하는지 확인

---

## 1. 사본 존재 여부

| 경로 | 상태 |
|---|---|
| `../hub/docs/` | 디렉터리 없음 — 사본 0건 |
| `../script-agent/docs/` | 디렉터리 없음 — 사본 0건 |

phase0-cleanup 완료(2026-06-05, `docs/phase0-snapshot/` 단일화) 이후 hub/script-agent의 `docs/` 사본이 monitoring-meta 단일 기준 문서로 이관·확정된 상태이므로 이는 정상이다. 사본 파일이 존재하지 않으므로 사본 drift 비교 대상 자체가 없다.

---

## 2. 이번 정정 내용 — 기준 문서 현행화 확인

이번 work-id phase1-044에서 아래 5곳이 정정됐다. 정정 결과가 기준 문서에 실제 반영되어 있는지 확인한다.

### 2.1 `docs/kafka-payloads.md` (2건)

| 위치 | 정정 전 (stale) | 정정 후 (현행) | 확인 결과 |
|---|---|---|---|
| 도입부 3행 | `v0_9/04_데이터흐름.md` (구 분할파일 경로) | `docs/master-design.md §6.9` | **현행화 완료** — 현재 파일 3행: `docs/master-design.md`(통합본) 6.8 참조 |
| 10행 (ADR#5 §, zone 언급) | `통합본 §13_open §A` | `통합본 13장 §A` | **현행화 완료** — 현재 파일 10행·36행: `통합본 13장 §A` 표기 확인 |

### 2.2 `docs/envelope.md` (3건)

| 위치 | 정정 전 (stale) | 정정 후 (현행) | 확인 결과 |
|---|---|---|---|
| Open 항목 표 §9 O2 | `13_open §E` | `13장 §E` | **현행화 완료** — 현재 파일 194행: `통합본 6.8.6, ADR #1, 13장 §E` 확인 |
| §9 미결 사항 | `§13_open §A` | `통합본 13장 §A` | **현행화 완료** — 현재 파일 36행: `통합본 13장 §A zone topology` 확인 |
| 통합본 참조 | `13_open.md` | `13장(Open)` | **현행화 완료** — 기존 분할파일명(`13_open.md`) 미잔존 확인 |

---

## 3. 형제 repo 사본 내 구 분할파일 참조 잔존 여부

### 3.1 검사 대상 패턴

이번 정정에서 제거된 구 패턴:
- `v0_9/` (구 분할 디렉토리 경로)
- `04_데이터흐름` (구 분할파일명)
- `13_open` (구 분할파일명)
- `통합본_v0_9` (구 통합본 파일명)
- `§13_open` (구 §-참조 표기)

### 3.2 검사 결과

`../hub/docs/`와 `../script-agent/docs/` 디렉토리가 존재하지 않으므로 사본 파일 내 stale 참조 잔존 여부 검사를 수행할 수 없다.

**판정: 사본 부재 — drift 검사 대상 없음**

---

## 4. 형제 repo 에이전트 설정 파일 내 구 분할파일 참조

참고로, 형제 repo 에이전트 설정 파일(`.claude/`, `AGENTS.md` 등)에서 `통합본_v0_9.md` stale 경로·호칭은 이전 보고서(`spec-drift-20260612-000000.md` §2.1)에서 이미 목록화·발주됐다. 이번 정정 대상인 구 분할파일명(`v0_9/04_데이터흐름.md`, `13_open.md`, `§13_open`)과는 별개 항목이다.

형제 repo 에이전트 파일 접근이 불가능하므로 구 분할파일명 패턴의 잔존 여부를 직접 확인할 수 없다. 단, 2026-05-27 이전 spec-drift 보고서(spec-drift-20260527-000000.md, spec-drift-envelope-20260527-143000.md) 기준으로 형제 repo 에이전트 파일이 `kafka-payloads.md`나 `envelope.md`의 내부 §-참조를 직접 인용하는 사례가 발견되지 않았으므로 위험도는 낮다고 판단한다.

**다만 이는 추측이 아닌 "과거 보고서에서 해당 패턴 발견 없음"을 근거로 한 판단이며, 직접 확인한 것이 아님을 명시한다.**

---

## 5. 종합 판정

| 항목 | 판정 | 비고 |
|---|---|---|
| 기준 문서 `kafka-payloads.md` 구 분할파일 참조 5건 현행화 | **기준 문서 현행화 완료** | 정정 확인 |
| 기준 문서 `envelope.md` 구 분할파일 참조 3건 현행화 | **기준 문서 현행화 완료** | 정정 확인 |
| 형제 repo 사본 `../hub/docs/` | **사본 부재 — drift 검사 없음** | phase0-cleanup 이후 정상 상태 |
| 형제 repo 사본 `../script-agent/docs/` | **사본 부재 — drift 검사 없음** | phase0-cleanup 이후 정상 상태 |
| critical / conflict 항목 | **없음** | |

**escalation 필요 항목**: 없음.

---

## 6. 잔존 추적 항목

이번 검사에서 신규 drift는 발견되지 않았다. 기존 발주 대기 중인 항목은 `spec-drift-20260612-000000.md` §7 잔존 항목 책임·트리거 표를 참조한다.
