# 셸 스크립트는 LF 고정 — CRLF로 체크아웃되면 Git Bash가 셰뱅(#!/usr/bin/env bash\r)을 못 읽어 hook이 깨진다.
*.sh text eol=lf
# bash가 source하는 profile도 LF 고정 — CRLF면 변수/경로 끝에 \r이 붙어
# 문맥 문서를 조용히 건너뛴다 (proposal-review.profile, codex-gate.profile 등)
*.profile text eol=lf
